# Methods log (moved)

This file has been combined into the single authoritative methods log at
[`docs/methods_log.md`](../docs/methods_log.md).

Its former content (the 3D ground-location methods M0 to M11 and the identity methods ID-0 to ID-6) is
now Parts B and C of that file, with the same numbers. The full original prose is preserved in git
history (`git log --follow wip/methods_log.md`).
